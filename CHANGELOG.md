# 更新紀錄

版本號同時寫在 `manifest.json`、擴充套件名稱、儀表板標題與標題列徽章上。
**資料夾名稱刻意不帶版本號**——未封裝擴充套件的 ID 由資料夾路徑算出，改名等於變成另一個擴充套件，既有資料不會跟過來。

## v1.2.2 — 2026-08-13

### 修正：路徑樹跟步驟表畫的根本不是同一條路

規劃器算完之後，儀表板要靠每一步的 `key` 去認「這一步用的是哪一條配方」，
但**規劃器產出的步驟從來沒帶 `key`**。比對永遠失敗，於是路徑樹只好自己照
「全域最短深度」挑一條畫——跟上面 1. 2. 3. 的步驟表是兩條不同的路。

同一個原因也讓「**另有 N 條配方 ⇄**」**第一次點必定沒反應**（找不到目前用的是哪一條，
就把它換成同一條）。兩個都修好了：模式（最短路徑／最少未知）現在真的會影響樹的長相。

### 修正：走不通的循環路徑被印成「照這個順序做」

配方表裡真的有互相循環的東西（A 要 B、B 又要 A）。以前畫面會照樣寫
「3 步，但缺 1 樣材料」並編號 1. 2. 3.，但那個順序照著做不出來——
第 2 步要用到第 3 步才生得出來的東西。現在：

- 標題改成「**這條路走不通**」，並指出是哪個造物在循環
- 配方**不編號**（改用 `・`），並直說「排不成可以照做的順序」
- 排不出順序的步驟也會列出是卡在哪幾個造物

### 修正：儀表板的「重新整理」看起來沒反應

它其實有重讀資料庫，但**不會重畫「怎麼煉」那一頁**，而且按下去完全沒有回饋，
所以看起來像壞的。現在會連同路徑一起重畫，按鈕也會顯示「重新整理中…／已重新整理」。

### 其他

- 新增 `tests/plan-test.html`（52 項）：直接拿渲染出來的 DOM 逐項比對步驟表與路徑樹，
  這三個 bug 都是它找出來的，也都在修好前驗證過會紅
- 清掉兩處重複呼叫的 `renderPlanBasis()`
- 離線自我測試 298 → 352 項斷言

## v1.2.1 — 2026-08-13

### 修正：浮層的「有新版本」按鈕根本不會出現

v1.1.3 加的版本提示，**在遊戲內浮層上是壞的**——按完「⟳ 更新」不會冒出來。
（儀表板那顆一直是好的。）

原因是資料流斷了一截：背景查完版本後，把結果放在**廣播**裡送出，
但 `chrome.runtime.sendMessage` 從 service worker 送出時**送不到 content script**，
浮層讀的又是 content script 自己那份進度物件——那裡永遠沒有版本資訊。

現在改成背景把結果**回覆**在「進度回報」那條訊息上，content script 收到後
轉成事件給浮層。儀表板走的廣播那條沒有變。

> 影響：v1.1.3 與 v1.2.0 的使用者，浮層要**重新整理遊戲分頁**之後才看得到提示
> （那時會讀上次存下來的檢查結果）。升到這一版就會在按完更新的當下直接出現。

### 其他

- 補上三處回歸測試，把這條路徑的每一段都釘住：背景的回覆要帶結果、
  content script 要真的轉發、浮層**不准**再去讀進度事件裡的版本資訊。
  三個測試在修好之前都會紅
- 離線自我測試 294 → 298 項斷言

## v1.2.0 — 2026-08-13

### 新增：目標——想試但還沒魔力試的組合

在遊戲裡選好兩樣材料，浮層判定那一行的**最右邊多了一顆星星**，預設是暗的。
點亮 ★ 就記下來，之後有魔力再回頭試。判定列下面多了一條**預設收合**的「目標」，點開就是整份清單。

- 清單**只存材料，不存結果**——每次展開都用當下的共用配方表重算。
  所以匯入別人的配方之後，原本寫「尚無紀錄」的目標會自己變成有答案
- 已知的直接寫出判定與產物（`✅ 已知可成 → 🌋 地熱`），沒人試過的寫 `❓ 尚無紀錄`
- 每筆右邊 **✕** 移除；移掉的正好是你現在選著的那組時，星星會跟著暗下去
- 收合狀態會記住，標題顯示目前有幾個
- **跨帳號共用**（跟共用配方表同一層）：切帳號待辦清單不會消失，也不必等第一次「更新」
- 試過之後不會自動移除——結果會顯示在那一列，看完自己 ✕ 掉。
  崩解之後你可能還想改用祈禱再試一次，自動刪掉反而礙事

### 其他

- IndexedDB 升到 v3，多一個 `goals` 資料表；舊資料不動
- 「完全重置」會連目標一起清掉，訊息也會寫出清了幾筆
- 背景服務測試 73 → 90 項、浮層 46 → 73 項，離線自我測試共 294 項斷言

## v1.1.3 — 2026-08-13

### 新增：有新版本會告訴你

按「⟳ 更新」跑完時，順便問一次 GitHub 的 `releases/latest`。**比你現在裝的新**才會浮出一顆
「**⬆ 有新版本 vX.Y.Z**」——浮層與儀表板標題列各一顆。按下去開發布頁，要不要更新你自己決定。

- **只在按「更新」時查**，而且**一小時內只查一次**
- 開儀表板或遊戲分頁只讀上次查到的結果，不會自己去打 GitHub
- 查不到（斷網、GitHub 掛了、被限流）就當作沒這回事，不會害更新看起來失敗
- 草稿與預發布不算數；版本比較是逐段比數字，`1.10.0` 比 `1.9.0` 新

> **不會自動下載、也不會自動安裝。** Chrome 不允許擴充套件改寫自己的檔案，
> `chrome.runtime.requestUpdateCheck()` 也只對商店安裝的擴充套件有效。
> 更新一律是：下載 zip → 解壓覆蓋**同一個資料夾** → `chrome://extensions` 按「重新載入」 → 遊戲分頁 F5。

### 其他

- `host_permissions` 多了 `https://api.github.com/*`（只讀公開的版本號，不送出任何東西）
- 新增版本比較測試（22 項），背景服務測試 57 → 73 項、浮層 39 → 46 項，離線自我測試共 250 項斷言

## v1.1.2 — 2026-08-13

### 修正：浮層終於讀得到你在遊戲裡選的材料

原本找的是 `button.chip.selected`，那是**材料列表**那排按鈕，不是**煉製台的格子**——
所以幾乎永遠抓不到，浮層就一直退回手動輸入。現在改讀煉製台本身：

```html
<div class="workbench"><div class="slots">
  <div class="slot filled"><div class="chip r3"><span>🧟</span><span>殭屍</span></div></div>
  <span class="op">＋</span>
  <div class="slot">…</div>
</div></div>
```

你把材料放進格子，**兩個框就自動填上**，判定立刻出來；拿掉就跟著清掉。

順帶修掉另一個會吃掉材料的問題：收藏過的造物，chip 裡會多一個裝星星圖示的空 `<span>`，
舊程式一律取「最後一個 span」，就會取到那個空的，那樣材料等於整個消失。

### 其他

- 只放一樣材料時，遊戲那顆按鈕是「萃取」不是「煉成」，判定現在會標成 `萃取：`，不會看錯
- **偵測不到煉製台時不動你打的字**——切到別的分頁，框裡的查詢會留著
- 自動填入的框線比較亮，一眼分得出是讀來的還是自己打的
- 拿掉浮層裡重複顯示材料的那一列（輸入框已經寫著同樣的東西）
- 遊戲改版時的退路仍在：抓不到煉製台就改讀 `.chip.selected`，再抓不到就純手動
- 浮層測試 28 → 39 項，離線自我測試共 205 項斷言

## v1.1.1 — 2026-08-13

### 浮層瘦身：只留「按下去之前該知道的事」

遊戲內浮層拿掉「**現在手上就能煉**」清單。那是慢慢看的東西，不該長期擋在遊戲畫面上，
而且它每分鐘會跟背景要一次資料。同一份清單在儀表板的「怎麼煉」頁本來就有，功能沒有少。

現在浮層只剩：材料兩格、判定結果、⟳ 更新、▤ 儀表板、－ 收合。

### 新增：浮層上的全服動態收集開關

不用開儀表板就能撥。跟「資料」頁那顆**是同一個設定**（存在 `chrome.storage.local`），
兩邊即時同步，撥哪一邊都一樣。

### 其他

- 清掉背景服務裡跟著沒人用的 `suggest` 指令與其輔助函式（儀表板本來就直接讀資料庫）
- 新增浮層測試（28 項），離線自我測試 166 → 194 項斷言

## v1.1.0 — 2026-08-13

### 新增：全服動態收集（預設關閉）

「資料」頁多了一個開關。打開後只要遊戲分頁開著，就會**每 30 秒**讀一次遊戲的
「全服最新的合成紀錄」（`/api/ranking/recent`），把所有玩家的配方收進共用配方表。

- **只進共用配方表，不會進你的軌跡**——那些是別人煉的
- 發現者照遊戲回報的名字記，不會掛上你的帳號
- 崩解的紀錄一樣收
- 開好幾個遊戲分頁只有一個會輪詢，不會變成好幾倍請求
- 同一筆看過就不再處理
- **預設關閉**，開關即時生效；「資料」頁會顯示已讀取幾次、併入幾組配方

### 修正

- 全服動態的去重判斷用 `!id`，會把 `id` 為 `0` 的那一列吃掉，改成 `id == null`

### 其他

- 端點白名單測試補上 `/ranking`，之前新增的端點不會被檢查到
- 新增輪詢器測試（9 項），離線自我測試總數 143 → 166 項斷言

## v1.0.1 — 2026-08-13

首次發布。

### 共用配方表（核心）

- 跨帳號共用的配方知識：**普通與祈禱分兩軌**記錄，成功與失敗都收
- 六種判定：已知可成／普通會崩解但祈禱可成／祈禱煉得出來／已知會崩解／兩者都失敗過／尚無紀錄
- 後來的成功會覆蓋先前的失敗；偶發的失敗不會把已知的成功降級
- 三種來源都標記出來：自己煉的、更新從遊戲收回來的、別人分享匯入的

### 配方共享

- **分享用匯出**：只出配方表，不含軌跡、素材櫃與帳號 id
- **匯入合併**：以材料組合為主鍵，同一組配方併進同一筆而非新增；對方的成功會救回你原本的崩解，你自己煉到的產物不被覆蓋，最早的發現者保留

### 遊戲內浮層

- 在工坊選材料的當下就顯示已知結果
- 「現在手上就能煉」清單
- 抓不到選取狀態時自動降級為手動輸入，不影響記錄功能

### 怎麼煉

- AND/OR 圖上的 Dijkstra 規劃器，成本為 `(缺料數, 步數)`
- 兩種模式：最短路徑／最少未知
- **步驟表與路徑樹畫同一條路**，樹會停在你已持有的節點
- 同一造物有多條做法時可點著換，步驟表跟著重算

### 帳號與更新

- 以 `/api/me` 的 `id` 分辨帳號，改名號不會讓紀錄斷成兩個人
- 配方跨帳號共用；持有物、金錢、魔力、軌跡分帳號
- 「⟳ 更新」只讀六支「我自己的」端點，**不會逐一走訪你的造物**
- `/combine-log` 會探測 `limit` 與 `offset`，伺服器不支援就立刻停手

### 其他

- 診斷按鈕：把每支端點的原始回應攤開，看得出是權限問題還是欄位改名
- 完全重置：回到剛安裝的狀態，方便測試
- 資料只存在本機 IndexedDB，除了你按下「更新」或「診斷」，不會主動對遊戲發任何請求

### 測試

離線自我測試共 143 項斷言，外加儀表板六個分頁的實際渲染與診斷報告格式驗證。
（v1.1.0 起為 166 項。）
